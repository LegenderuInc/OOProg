#if !defined(__Enum_h)
#define  __Enum_h

enum EiendomsType {
    tomt, enebolig, rekkehus, leilighet, hytte
};

enum Onske {
    salg, leie, begge
};

enum Boliginfo {
    ukentlig, snarest
};

#endif